// components
import { Border, Grid } from "../components";
import Head from 'next/head';

// widgets
import { LandingSection, ContactSection, SkillsSection, AboutSection, FooterSection } from "../widgets";
import { useTheme } from "styled-components";
import styled from "styled-components";

const StyledHome = styled.div`
  .Home__AboutBottomBorder {
    width: 90%;
  }
  
  @media (min-width: 1025px) {
    .Home__AboutBottomBorder {
      width: 50%;
    }
  }
`

export default function Home() {
  const theme = useTheme();
  return (
  <StyledHome>
    <Grid
      direction="column wrap"
      bgColor={theme.color.secondary.value}
      alignItems="center"
    >
      <Head>
        <title>Alexis Marroquin</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <LandingSection/>
      <SkillsSection/>
      <AboutSection/>
      <Border 
        className="Home__AboutBottomBorder"
        bgColor="black"
      />
      <ContactSection/>
      <FooterSection/>

    </Grid>
  </StyledHome>
  )
}
