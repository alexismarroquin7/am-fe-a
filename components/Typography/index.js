
export const Typography = (props) => {
  
  if(props.variant === 'h1'){
    
  } else if(props.variant === 'h2'){
    
  }
  else if(props.variant === 'h3'){
    
  }
  else if(props.variant === 'h4'){
    
  } else if(props.variant === 'h5'){
    
  } else if(props.variant === 'h6'){
    
  } else {
    throw Error('variant prop is required');
  }
  
}