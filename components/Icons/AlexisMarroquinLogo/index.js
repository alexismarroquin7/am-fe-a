export const AlexisMarroquinLogo = ({onClick}) => {
  return (
  <button
    className="wrapper"
    onClick={onClick}
  >
    
    AM
    <style jsx>{`
      .wrapper {
        border: 0;
        background-color: transparent;
        font-size: 2rem;
        

        color: var(--secondary);
      }
    `}</style>
  </button>
  )
}